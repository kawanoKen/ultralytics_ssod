# 2026-09-07 作業まとめ

## 1. SSOD実装の追加

### weak/strong augmentation分離
teacherは弱augmentation画像(`img`)、studentは強augmentation画像(`img_strong`)を見るように分離。両者は同じ幾何変換・ラベルを共有するため座標変換は不要。

- `ultralytics/data/dataset_ssod.py`: `StrongAugment`クラス追加(color jitter, grayscale, blur, cutout)。`YOLODataset_ssod.collate_fn`で`img_strong`もstack対象に追加
- `ultralytics/models/yolo/detect/ssod_train.py`: teacherは`img`、studentは`img_strong`で推論するよう変更
- `ultralytics/cfg/default_ssod.yaml`: `strong_*`系ハイパラ追加

### DFL localization confidence
`dfl_ssod_algorithm_flexible.md`のコア案(edge confidence→box confidence=4辺のmin→pseudo-label品質判定に利用)を実装。

- 新規: `ultralytics/utils/dfl_confidence.py` (`localization_confidence()`)
- `ultralytics/utils/loss_ssod.py`: `EfficientTeacherLoss`に`use_loc_conf`/`loc_conf_threshold`追加。reliable判定を「cls_conf高 AND loc_conf高」に拡張(Option B)
- `ultralytics/models/yolo/detect/ssod_train.py`: teacher forwardの生特徴からDFLロジットを復元、NMSに`return_idxs=True`を指定してanchor対応を維持し、pseudo-boxごとにloc_confを計算
- `ultralytics/cfg/default_ssod.yaml`: `use_loc_conf: True`, `loc_conf_threshold: 0.6`

上記はコミット・`origin/main`にpush済み。

## 2. VOCデータセット準備

- PASCAL VOC(07+12)を`/work/kawano/LA/datasets/VOC`にダウンロード・YOLO形式変換(約5.6GB)
- `ultralytics/cfg/datasets/VOC_ssod.yaml`: labeled=VOC07 trainval(5011枚)、unlabeled(ssod_train)=VOC12 trainval(11540枚)、eval=VOC07 test(4952枚)

## 3. 教師ありbaseline(labeled-onlyの収束確認)

`scripts/voc_baseline/train_labeled_baseline.sh` (COCO pretrained→VOC07 trainvalでfine-tune、100epoch、4GPU DDP、batch=384)

| model | mAP50 | mAP50-95 |
|---|---|---|
| yolov8n | 0.795 | 0.578 |
| yolo11n | 0.799 | 0.589 |

重み: `runs/voc_labeled_baseline/{yolov8n,yolo11n}_voc07_labeled/weights/best.pt`

## 4. Batch sizeベンチマーク

- 教師あり単体: `scripts/voc_baseline/bench_batch_size.sh` → 単GPUでbatch=128が最速(256はOOM)
- SSOD(pseudo-label)フェーズ: `scripts/voc_ssod/bench_one_ssod_batch.py` → 単GPUでbatch=64/64が安全(128/128はOOM)。本実行は2GPU DDP、batch=128/batch_ssod=128(=64/GPU)を採用

## 5. SSOD比較実験(YOLOv8n、進行中)

burn-inは省略し、3のbest.ptから直接pseudo-labelフェーズを開始。

- **① baseline**: `conf_threshold_high=0.5, conf_threshold_low=0.3, use_loc_conf=False`
- **② dfl-aware**: 同conf + `use_loc_conf=True, loc_conf_threshold=0.6`
- 100epoch, GPU0,1(①)/GPU2,3(②)で並列実行
- 起動スクリプト: `scripts/voc_ssod/train_ssod.py --variant {baseline,dfl}`
- 完了後、同条件でYOLOv11nも自動追撃実行するようシーケンサー(`scripts/voc_ssod/run_yolov11_after_yolov8.sh`)を仕込み済み
- 出力先: `runs/voc_ssod/{yolov8n,yolo11n}_voc_ssod_{baseline,dfl}/`(重みは`weights/`、pseudo-label可視化は10epochごとに`epoch{N}/pseudo_batch{0-3}.jpg`)

### 実装中に見つけたDDPバグ(修正済み)
1. `EfficientTeacherLoss`にDDPラップ後の`self.model`を渡していたため`model.args`アクセスが失敗 → `unwrap_model(self.model)`を渡すよう修正
2. 1イテレーション中に同じDDPラップ済みモデルを2回forward(labeled→unlabeled)していたため、DDPのbackward準備処理がautogradのバージョン管理と衝突しRuntimeErrorが発生 → student側(2回目)のforwardをDDPの生モジュール経由に変更して解消
3. pseudo-label可視化が全epoch×全batchで同期保存されディスク肥大化・速度低下の原因になっていた → `save_period`ごと・各epoch先頭4バッチのみに間引き

**現時点(21:59時点)ではepoch 3/100まで安定進行中、クラッシュなし。完了は数時間後の見込み。** 最終mAP等の数値は実行完了後に追記予定。

## 6. 最終結果(全実験完了、2026-09-08 11:18時点)

| model | variant | mAP50 | mAP50-95 | Precision | Recall |
|---|---|---|---|---|---|
| yolov8n | 労働ありbaseline(参考) | 0.795 | 0.578 | - | - |
| yolov8n | ① cls confidenceのみ | 0.8147 | 0.6075 | 0.820 | 0.729 |
| yolov8n | ② cls + DFL loc confidence | 0.8144 | 0.6079 | 0.809 | 0.738 |
| yolo11n | 労働ありbaseline(参考) | 0.799 | 0.589 | - | - |
| yolo11n | ① cls confidenceのみ | 0.8260 | 0.6283 | 0.816 | 0.742 |
| yolo11n | ② cls + DFL loc confidence | 0.8225 | 0.6264 | 0.819 | 0.742 |

- 両アーキテクチャともSSOD(VOC12 unlabeledのpseudo-label活用)で労働ありbaselineから明確に改善(yolov8n: mAP50 +0.02、yolo11n: mAP50 +0.027)。
- yolo11nの方がyolov8nよりSSOD後の最終mAP50/50-95が高い。
- ①(cls confidenceのみ)と②(DFL loc confidence追加)の差は両モデルとも僅少。今回の閾値設定(conf_threshold_high=0.5, conf_threshold_low=0.3, loc_conf_threshold=0.6)では②が明確に優位という結果にはならなかった(mAP50は①がわずかに上、Recallは②がわずかに上)。閾値の再探索や別データセットでの追試が今後の課題。
- 重み・ログ: `runs/voc_ssod/{yolov8n,yolo11n}_voc_ssod_{baseline,dfl}/`
