# Experiment 1: Pseudo-Label Qualityを揃えたとき、どのInstanceを学習するかは重要か

## 0. 目的

半教師あり物体検出において、擬似ラベルの

* 個数
* 真の品質

を揃えたとしても、

**「どのinstanceを学習対象として選ぶか」**

によって学習結果が変わるかを検証する。

中心仮説は以下。

$$
\boxed{
\text{Pseudo-label qualityが同程度でも、
既存の教示で十分にcoveredされていないinstanceを学習する方が有益である}
}
$$

今回の実験では新しいselectorを提案しない。

GTを診断目的で利用し、

> そもそも「品質以外のinstance selection」に改善余地が存在するか

だけを確認する。

---

# 1. 最重要ルール

## Design freeze

Full run開始前に以下を固定する。

* teacher checkpoint
* labeled / pseudo-unlabeled / evaluation split
* pseudo-label生成設定
* GT matchingルール
* quality threshold
* quality bin
* pseudo-label budget
* coverage descriptor
* selection algorithm
* optimizer
* learning rate
* augmentation
* supervised / unsupervised loss weight
* update数
* seed

Full runを見てからthresholdやbinを変更しない。

---

## 過去の不正なSSOD checkpointは使用しない

EMA更新不具合があった旧SSOD runは使用禁止。

使用可能なのは、

* 正常に学習された supervised-only checkpoint
* EMA修正版で正常性が確認されたcheckpoint

のみ。

特に、diagnostic pseudo-unlabeled poolを過去にpseudo-label学習へ使用したSSOD checkpointは避ける。

最も望ましいのは、

> **対象labeled splitだけで学習したsupervised checkpoint**

を固定teacherとして使うこと。

---

# 2. 最初に使う設定

第一実験は **1% labeled** を優先する。

理由：

* 少量GTでもteacherがある程度動いていることが確認できている
* pseudo-label selectionの必要性が最も現れやすい
* 「少量labeled dataからどこまで学習範囲を広げられるか」という研究目的に合う

ただし、oracle-correct pseudo-label候補が極端に少ない場合のみ10%条件から開始してよい。

1%で効果が確認された場合、10%をconfirmation experimentとして追加する。

---

# 3. Dataset split

CrowdHuman training imagesを以下に分ける。

```text
D_L      : labeled training set
D_U_diag : pseudo-unlabeled diagnostic training set
D_eval   : evaluation set
```

`D_U_diag` はGTを持っているが、

**teacher推論・student学習時にはGTを絶対に入力しない。**

GTは、

* pseudo-label quality判定
* oracle selection
* diagnostic analysis

にのみ使う。

`D_eval` はselection・threshold調整に一切使わない。

画像単位で完全分離すること。

---

# 4. 固定Teacher

既存checkpointを

$$
T_0
$$

とする。

このExperiment 1では、

$$
\boxed{T_0\text{ を最後まで固定する}}
$$

EMA updateもteacher refreshも行わない。

全branchで完全に同一のteacherを使う。

Studentも全branch、

$$
\theta_0
$$

という同一checkpointから開始する。

可能なら

```text
teacher = T0
student init = T0
```

でよい。

---

# 5. Pseudo-label生成

`D_U_diag` 全画像に対して一度だけpseudo-labelを生成し、ファイルへ保存する。

以後、学習中にはpseudo-labelを再生成しない。

現在のpipelineに合わせ、

* image size: 640
* NMS IoU: 0.65
* deterministic inference
* 同じpreprocessing

を使用する。

候補を十分確保するため、confidence thresholdは通常の採用thresholdより低くする。

初期候補として、

```text
conf >= 0.05
```

を推奨する。

ただし既存pipeline上これが不適切なら、Full run前に一度だけ決定して固定する。

各candidateについて最低限保存する。

```text
image_id
box
teacher_confidence
teacher raw outputs if available
```

---

# 6. GTとのOracle Matching

これは診断専用。

各画像についてpseudo boxとGTをone-to-one matchingする。

CrowdHumanで現在学習に利用しているGT box定義をそのまま用いる。
例：現在full-body boxをtargetとしているならfboxを使用する。

matching procedure:

1. predictionとGTのIoU matrixを計算
2. predictionをteacher confidence降順に処理
3. unmatched GTの中で最大IoUのGTへmatch
4. IoU < 0.5ならunmatched
5. 1 GTにつきpredictionは1個まで

各candidateについて、

```text
matched_gt_id
IoU_to_GT
is_true_positive
```

を保存する。

---

# 7. Oracle-correct candidate pool

今回のExperiment 1では、

$$
IoU \ge 0.5
$$

でone-to-one matchできたpseudo boxだけを、

```text
C_valid
```

とする。

今回は、

> 「誤pseudo-labelをどう除くか」

を調べる実験ではない。

したがって、FPは学習候補から除外する。

これはoracle experimentであり、最終手法ではないことを明記する。

---

# 8. Quality bin

Pseudo-labelの真のlocalization qualityを揃えるため、

```text
[0.50, 0.60)
[0.60, 0.70)
[0.70, 0.80)
[0.80, 0.90)
[0.90, 1.00]
```

の5binに分割する。

全selection methodで、

**各IoU binから選択するpseudo box数を完全に同じにする。**

つまり、

$$
N_k^{A}
=
N_k^{B}
=
N_k^{C}
$$

for every IoU bin \(k\)。

これによって、

> Coverage methodが単に高品質pseudo-labelを多く選んだから強かった

という説明を排除する。

---

# 9. Pseudo-label budget

総採用pseudo-instance数を

$$
B
$$

として全条件で完全に揃える。

BはFull run前に固定する。

推奨：

現在のbaseline SSODが同じ画像集合に対して通常採用するpseudo-label数と同程度。

ただし各quality binに十分なcandidateが存在することを確認する。

budgetが決定したら変更しない。

---

# 10. Instance condition descriptor

Experiment 1では、**GTを使ったoracle descriptorでよい。**

目的は手法を作ることではなく、

> instance coverageを変える価値が存在するか

を確認することだからである。

各matched GTについて以下を計算する。

### 10.1 Object scale

$$
z_1 =
\log
\frac{\text{fbox area}}
{\text{image area}}
$$

### 10.2 Visibility

CrowdHumanのvisible boxを使い、

$$
z_2=
\frac{\text{vbox area}}
{\text{fbox area}}
$$

とする。

小さいほどocclusionが強い。

### 10.3 Neighbor overlap

対象GTと他のGTについて、

$$
z_3 =
\max_{j\neq i}
IoU(fbox_i,fbox_j)
$$

を計算。

### 10.4 Local crowding

対象box中心付近の一定領域に存在する他GT数など、

既存実装で簡単に定義できる局所密度指標を1つ使用する。

最初は複雑にしない。

最終descriptor：

$$
z_i=
[z_1,z_2,z_3,z_4]
$$

各dimensionを `D_U_diag` 内で標準化する。

---

# 11. Labeled support

`D_L` 内の全GTについても同じdescriptor

$$
z_l
$$

を計算する。

Labeled dataが既にcoverしているinstance conditionを、

$$
Z_L=\{z_l\}
$$

として保持する。

---

# 12. 比較するSelection Method

最低3条件。

---

## A. CONF

各IoU quality bin内で、

```text
teacher confidence
```

の高い順に選択する。

各binで所定数だけ採用。

これは、

> qualityはoracleで揃えた上で、従来型のteacher confidenceを優先する

baseline。

---

## B. RANDOM

各quality bin内からランダムに選択。

seed固定。

これは、

> instanceを選び分けること自体に意味があるか

を見るbaseline。

---

## C. COVERAGE

各quality bin内で、

> labeled dataですでにcoverされていないinstance condition

を優先する。

単純なgreedy farthest-first selectionを使用する。

初期support：

$$
S=Z_L
$$

各stepで、

$$
i^* =
\arg\max_i
\min_{z\in S}
\|z_i-z\|_2
$$

を選択。

選択後、

$$
S\leftarrow S\cup\{z_{i^*}\}
$$

とし、必要数に達するまで繰り返す。

つまり、

* labeled dataから遠い
* かつ既に選んだpseudo-instanceとも重複しすぎない

ものを選択する。

---

# 13. Optional negative control

計算予算に余裕があれば、

## D. NEAR

を追加する。

各quality bin内で、

$$
d_i=
\min_{z_l\in Z_L}
\|z_i-z_l\|
$$

が小さい順に選ぶ。

つまり、

> labeled dataですでに十分coverされている条件

を意図的に優先する。

予想される関係を、

```text
COVERAGE > RANDOM / CONF > NEAR
```

と仮説として置ける。

ただしこれは仮説であり、結果に合わせて変更しない。

---

# 14. 学習時の最重要事項

この実験で最も注意すること。

**選ばなかったpseudo-objectをbackgroundとして学習してはいけない。**

そうすると、

```text
instance selection
```

と

```text
false-negative background supervision
```

が混ざる。

Experiment 1ではunlabeled branchについて、

$$
\boxed{\text{selected pseudo-object由来のpositive supervisionのみ使用}}
$$

する。

具体的には、

* selected pseudo boxesのみpositive assignmentに使用
* classification / box / DFL lossはselected positivesのみ
* unlabeled画像の未選択領域にnegative/background lossを掛けない
* selectedされなかったpseudo boxesをbackground labelへ変換しない

とする。

これは通常SSODとは異なる**診断専用設定**である。

Labeled branchは通常通り全lossを計算する。

---

# 15. Loss normalization

全branchでunsupervised lossの総量が大きく変わらないようにする。

pseudo-label数 \(B\) は同じなので、

基本的には同じnormalizationを使用する。

最低限logする。

```text
selected pseudo boxes / batch
positive assigned predictions / batch
unsupervised cls loss
unsupervised box loss
unsupervised DFL loss
total unsupervised loss
```

異なるselection methodでloss scaleが極端に違わないことを確認する。

後から係数を調整して結果を合わせない。

---

# 16. Training branches

すべて

```text
same initial checkpoint
same labeled batches
same pseudo-unlabeled images
same image order
same augmentations
same optimizer state
same learning rate
same total updates
same SSOD loss weight
```

にする。

異なるのは、

$$
\boxed{\text{どのpseudo-instanceへpositive lossを掛けるか}}
$$

だけ。

---

# 17. Smoke test

まず各条件、

```text
200 updates
seed = 0
```

程度。

目的は性能比較ではなく、

* NaN / infなし
* pseudo box数一致
* quality bin数一致
* background loss混入なし
* selected instanceの可視化が正しい
* branch間でlabeled batchが同じ
* branch間でaugmentationが同じ

を確認すること。

Smoke結果でmethodの優劣を判断しない。

---

# 18. Full run

Smoke通過後、

```text
2,000 updates
3 seeds
```

から開始する。

2,000は初期診断の目安。

差が学習曲線上でまだ拡大・縮小中なら、全条件を同じupdate数だけ延長する。

特定methodだけ延長しない。

---

# 19. Evaluation

Primary:

```text
mAP50-95
mAP50
Precision
Recall
```

を使用。

加えて、GT条件別に評価する。

### Scale

```text
small
medium
large
```

### Visibility

例：

```text
low visibility
medium visibility
high visibility
```

### Crowding / overlap

例：

```text
low overlap
medium overlap
high overlap
```

bin境界はFull run前に固定する。

---

# 20. Coverage diagnostic

各selection methodについて、

選択されたinstanceの

```text
object scale distribution
visibility distribution
neighbor overlap distribution
local crowding distribution
```

を出力する。

さらに、

$$
d_i=
\min_{z_l\in Z_L}
\|z_i-z_l\|
$$

の分布を出す。

期待する確認は、

```text
COVERAGE:
 labeled supportから遠いinstanceを多く含む

CONF:
 labeled support付近に偏る可能性

RANDOM:
 その中間
```

である。

この確認ができなければ、COVERAGE selectorの実装がおかしい。

---

# 21. 必須表

## Table 1 — Selected pseudo-label statistics

| Method | #PL | Mean IoU | IoU bins | Mean conf | Scale | Visibility | Overlap | Distance from labeled |
| ------ | --: | -------: | -------- | --------: | ----- | ---------- | ------- | --------------------: |

特に

```text
#PL
IoU bin distribution
```

が全methodで一致していることを確認する。

---

## Table 2 — Detection performance

| Method          | mAP50-95 | mAP50 | Precision | Recall |
| --------------- | -------: | ----: | --------: | -----: |
| CONF            |          |       |           |        |
| RANDOM          |          |       |           |        |
| COVERAGE        |          |       |           |        |
| NEAR (optional) |          |       |           |        |

3 seedsについて、

```text
mean ± SD
```

を報告する。

---

## Table 3 — Condition-wise performance

| Method | Small AP/R | Occluded AP/R | High-overlap AP/R | Easy AP/R |
| ------ | ---------: | ------------: | ----------------: | --------: |

APが計算しにくいsubsetではRecallでもよい。

---

# 22. 学習曲線

全条件について、

```text
update
vs
mAP50-95
```

を保存する。

加えて可能なら、

```text
update
vs
Recall
```

も保存する。

最終値だけでなく、

> Coverage selectionが収束を速めただけか
> 到達性能にも差があるか

を区別する。

---

# 23. 結果の解釈

## Case A

```text
COVERAGE > CONF ≈ RANDOM
```

かつpseudo-label qualityは同等。

→

**同じ品質・同じ量でも、どのinstanceを教えるかに価値がある。**

次のExperiment 2へ進む価値が高い。

---

## Case B

```text
COVERAGE > CONF
```

だが選択pseudo-labelのIoU分布も良い。

→

selection内容ではなくquality差の可能性。

Experiment 1のcontrol失敗。

quality matchingを修正する。

---

## Case C

```text
CONF ≈ RANDOM ≈ COVERAGE
```

→

少なくとも現在の設定では、

**正しいpseudo-labelなら、instance coverageを制御する追加価値は小さい。**

無理にこの方向を続けない。

quality estimation / missing pseudo-label / assignmentなど別要因へ戻る。

---

## Case D

```text
COVERAGE < CONF
```

→

非常に重要。

labeled supportから遠いinstanceは、

GT上pseudo-boxが正しくても、

* augmentation下で不安定
* feature learningが難しい
* localization noiseの影響が大きい
* current modelに対して過度に難しい

可能性がある。

この場合は、

> reliabilityだけでは十分ではない

という別の問題が見える。

---

# 24. 今回やらないこと

Experiment 1では以下は禁止。

* learned pseudo-label selector
* DFL uncertainty selector
* dynamic teacher update
* EMA teacher refresh
* curriculum
* density-based schedule
* loss-adaptive weighting
* active learning
* foundation model
* post-hoc threshold tuning
* methodごとの個別hyperparameter tuning

今回の目的は一つだけ。

$$
\boxed{
\text{品質と量を揃えても、
pseudo-label instance selectionに追加価値があるか？}
}
$$

を答える。

---

# 25. 最終出力

実験終了後、以下を1つのMarkdown reportにまとめる。

```text
1. Setup
2. Exact checkpoint used
3. Dataset split
4. Candidate generation
5. Oracle matching rule
6. Selection rule
7. Selected pseudo-label statistics
8. Overall detection results
9. Condition-wise results
10. Learning curves
11. Seed-wise results
12. Interpretation
13. Whether Experiment 2 is justified
```

最後に必ず、

```text
Does pseudo-label instance coverage matter
after controlling for pseudo-label quality and quantity?
```

に対して、

```text
YES / NO / INCONCLUSIVE
```

のいずれかで結論を書く。

結果に合わせて仮説を変更しない。
